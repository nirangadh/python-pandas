# In Google Colab, Pandas is pre-installed. To upgrade or install a specific version, you can run:
!pip install --upgrade pandas

# Verify the installation
import pandas as pd
print("Pandas version:", pd.__version__)
